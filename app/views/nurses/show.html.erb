<div class="top-hero-wrapper text-center">
  <div class="container top-hero-container">
    <h1 class="text-white"><%= @nurse.name %></h1>
    <div class="row">
      <div class="col-md-offset-2 col-md-2">
        <%= link_to 'Term', new_term_path(@nurse.id), class: 'btn btn-block btn-white' %>
      </div>
      <div class="col-md-offset-1 col-md-2">
        <%= link_to 'Hope_holiday', hope_holidays_path(@nurse.id), class: 'btn btn-block btn-white' %>
      </div>
      <div class="col-md-offset-1 col-md-2">
        <%= link_to 'Leave',  leaves_path(@nurse.id), class: 'btn btn-block btn-white' %>
      </div>
    </div>
  </div>
</div>
